# Vendor-docs allowlist for tests/unit/test_vendor_docs_alignment.py.
#
# Identifiers our shipped JS reaches but that legitimately don't appear
# in the omni-automation.com snapshot at tests/vendor-docs-snapshot/.
# One identifier per line. Comment lines start with #.
#
# When adding an entry, write the rationale on the comment line above.
# When the docs catch up, delete both lines.
#
# Note (2026-05-02): the lint now uses exact-set membership against
# parsed snapshot bullets, not substring search. The previous ~40-entry
# allowlist was inflated dead code — most entries had no effect because
# they were named with a `prefix.` form, while the lint compares only
# bare property names against the snapshot. The current minimal list is
# what's actually needed.

# JXA SDEF properties on the AppleScript Document object — NOT omniJS.
# Used inside src/omniplan_mcp/documents.py because the omniJS
# Document/PlanDocument class doesn't expose file URL or a `modified`
# accessor; we reach `app.documents()[0].path()` and `.modified()`
# through the AppleScript bridge instead.
modified
path

# JavaScript built-in Date methods used inside our omniJS fmtDate helpers.
# These belong to ECMAScript, not the omniJS class surface.
getUTCDate
getUTCFullYear
getUTCMonth

# Identifiers that appear only in Python docstrings (not in shipped JS).
# The lint extracts from every triple-quoted string in src/, which is
# over-broad on purpose — false-positives are cheaper than blind spots.
# `currency` is mentioned in documents.py docstring explaining why we
# don't ship it; `parent` is mentioned in tasks.py docstring explaining
# why parent_id is computed via traversal. Neither is reached at runtime.
currency
parent
